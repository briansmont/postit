<h1><%= @topic.title %></h1>

<div class="row">
  <div class="col-md-8">
    <p><%= @topic.body %></p>
  </div>
  <div class="col-md-4">
    <%= link_to "Edit", edit_topic_path(@topic), class: 'btn btn-success' %>
  </div>
</div>
